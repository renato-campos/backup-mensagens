[project]
name = "backup-mensagens"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14.2"
dependencies = [
    "markdown>=3.10.2",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.19.0",
]
